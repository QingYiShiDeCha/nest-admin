# nest-admin 新功能计划

更新时间：2026-09-02

## 当前目标

完成定时任务管理第一版：预注册任务、Cron 调度、启停、手动执行、执行日志和多实例防重。

## 设计决策

- 部门使用 `sys_dept` 自关联树，不保存冗余祖先路径，移动节点时只更新 `parent_id`。
- 一个用户最多直属一个部门，通过 `sys_user.dept_id` 关联。
- 角色的自定义数据范围通过 `sys_role_dept` 保存，不把部门 ID 数组塞进角色表。
- 多角色数据范围取并集；超级管理员始终可查看全部数据。
- 部门删除采用软删除；存在子部门或直属用户时拒绝删除。
- 部门停用后不可作为新用户归属部门，也不可作为新父部门。
- 部门负责人通过 `sys_dept.leader_id` 关联启用用户，接口按用户最新昵称或用户名展示。
- 用户停用、软删除或物理删除时自动解除负责人关系，避免部门保留失效用户。
- 岗位通过 `sys_post` 独立维护，用户岗位使用 `sys_user_post` 多对多关联。
- 岗位停用后不可新增分配，已有关系允许保留；岗位仍有用户时拒绝删除。
- 用户岗位查询和分配复用部门数据范围，不能通过用户 id 越权操作。
- 部门变更上级时必须填写迁移原因，普通资料更新不产生迁移记录。
- 部门更新与迁移历史在同一事务提交，并用原父级做乐观并发保护。
- 迁移历史保存部门、前后父级和操作人的名称快照，不受后续改名或删除影响。
- 普通用户角色权限与数据范围缓存到 Redis，超级管理员继续直接短路。
- 用户角色/部门和角色权限/范围变化递增用户版本，组织树变化递增全局版本。
- Redis 未配置或故障时回退数据库；缓存 TTL 默认 300 秒，限制失效失败的最长陈旧时间。
- 公告支持全员、部门、角色和指定用户四种接收范围，范围选择不写 JSON 数组。
- 发布时展开启用用户并生成收件人快照，后续组织与角色变化不改写历史。
- 已发布公告不可直接编辑或删除，必须先撤回；撤回后用户收件箱立即停止展示。
- 消息详情按收件人 id 与当前用户联合查询，不能通过猜测 id 查看他人消息。
- 已读操作不写操作日志，避免高频低价值记录淹没真正的管理审计。
- 实时通道使用 SSE，事件只表达数据失效，不承载公告正文，页面仍从 REST 接口获取权威数据。
- Redis Pub/Sub 负责多实例传播；未配置或故障时保留本实例直推，不阻断公告发布与已读操作。
- SSE 断线后自动重连并启用 60 秒未读轮询，恢复连接后停止轮询并主动同步一次数据。
- accessToken 过期时 SSE 与普通请求共用同一个 refreshToken 轮换任务，避免并发刷新触发盗用检测。
- 系统参数只保存非敏感业务配置，不替代 `.env` 中的数据库密码、JWT 密钥等部署配置。
- 参数值支持文本、数字、布尔和 JSON，写入时必须通过声明类型校验。
- 内置参数允许调整名称、值、状态和备注，但参数键不可修改，记录不可删除。
- 参数键全局唯一且软删除后不可复用，避免旧业务语义被新参数接管。
- 动态字典只承载业务人员可配置的选项，核心状态、菜单类型和数据范围继续使用 shared 静态枚举。
- 字典编码全局唯一，同类型字典值唯一，软删除后均不可复用。
- 业务读取只返回启用类型下的启用字典项，并按排序值和 id 升序。
- 字典缓存键携带编码版本，类型或字典项写入后主动递增版本，Redis 故障时回源数据库。
- 数据库只保存任务键与调度配置，真正执行逻辑必须在后端注册，不允许在线填写或执行任意代码。
- 同一预注册任务可配置多个计划，执行时按任务键共用 Redis 锁，防止多计划或多实例并发重复执行。
- 各实例启动时加载启用计划，并每 15 秒与数据库对账，使启停与 Cron 变更能够传播到其他实例。
- 手动执行立即返回运行中日志，任务在后台继续，避免长任务占用 HTTP 请求直到超时。
- 执行日志保存任务名称和任务键快照，记录定时/手动触发、状态、耗时、结果、错误与操作人。
- 内置日志清理计划首次创建时读取 `LOG_CLEANUP_CRON` 和 `LOG_CLEANUP_ENABLED`，之后以数据库管理页配置为准。

## 实施进度

- [x] 梳理现有用户、角色、菜单树与数据范围实现
- [x] 新增部门契约、权限码与数据库结构
- [x] 实现部门树 CRUD 与完整性校验
- [x] 接入用户部门字段和部门筛选
- [x] 接入角色自定义部门与数据范围过滤
- [x] 实现组织架构管理页面
- [x] 更新初始化数据、README 与路由菜单
- [x] 完成单测、类型检查、迁移校验、Lint 与构建
- [x] 新增岗位契约、权限码、数据库结构与迁移
- [x] 实现岗位 CRUD、占用保护和用户岗位分配接口
- [x] 实现岗位管理页面与用户岗位展示、分配弹窗
- [x] 更新初始化数据、README 与动态菜单入口
- [x] 新增部门迁移历史契约、权限码、数据库结构与迁移
- [x] 实现迁移原因校验、原子历史写入和并发保护
- [x] 实现迁移历史分页接口与前端历史抽屉
- [x] 新增授权与数据范围 Redis 缓存及版本票据
- [x] 接入角色、用户和部门写操作的主动失效
- [x] 增加缓存命中、并发失效、损坏数据与故障降级测试
- [x] 新增公告、定向范围和收件人快照契约、数据库结构与迁移
- [x] 实现公告草稿、更新、发布、撤回、删除和阅读统计接口
- [x] 实现我的消息分页、最近消息、未读计数和已读接口
- [x] 实现通知公告管理页、消息中心与 Header 未读入口
- [x] 增加通知权限码、默认菜单、规则测试和未读 Store 测试
- [x] 新增站内消息 SSE 契约、心跳端点和流式响应旁路
- [x] 实现 Redis Pub/Sub 多实例事件传播与本机故障降级
- [x] 接入公告发布、撤回、单条已读和全部已读事件
- [x] 实现前端 Bearer SSE、自动重连、Token 轮换和轮询降级
- [x] 增加用户隔离、Redis 回流去重、事件解析和连接降级测试
- [x] 新增系统参数契约、权限码、数据库结构与迁移
- [x] 实现参数分页、详情、新增、更新、删除和类型化读取服务
- [x] 实现参数配置管理页与动态值编辑控件
- [x] 增加内置参数、动态菜单和精选 Remix 图标
- [x] 新增字典类型/字典项契约、权限码、数据库结构与迁移
- [x] 实现字典管理 CRUD、状态排序和启用选项读取接口
- [x] 增加 Redis 字典缓存、版本票据、主动失效与故障降级测试
- [x] 实现数据字典管理页、动态菜单和可复用 `useDict(code)`
- [x] 增加示例业务字典并更新 README
- [x] 新增定时计划与执行日志契约、数据库结构与迁移
- [x] 实现预注册任务表、Cron/时区校验和动态调度
- [x] 实现 Redis 分布式锁、多实例对账和执行状态记录
- [x] 迁移日志与过期会话清理为首个内置任务
- [x] 实现定时任务列表、启停、手动执行和执行日志页面
- [x] 增加定时任务权限码、动态菜单和精选 Remix 图标

## 后续规划

- [x] 部门负责人改为可搜索的用户选择器
- [x] 增加岗位体系与用户岗位关系
- [x] 支持部门迁移审计与历史追踪
- [x] 为数据范围查询增加缓存和失效机制
- [x] 增加通知公告与站内消息第一版
- [x] 增加站内消息 SSE 实时推送与 Redis 多实例传播
- [x] 增加系统参数配置与内置参数保护
- [x] 增加数据字典管理、缓存与前端 `useDict`
- [x] 增加定时任务管理、执行日志与多实例防重
